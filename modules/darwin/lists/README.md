# Homebrew lists

These Homebrew lists were imported from the old setup and are now split by category so they are easier to scan.

## Shared

- `taps.nix`

## Formulae

- `brews-cli-shell.nix`
- `brews-dev-workflow.nix`
- `brews-language-toolchains.nix`
- `brews-data-infra-media.nix`

## Casks

- `casks-security-ai.nix`
- `casks-dev-browsers.nix`
- `casks-productivity-media.nix`
- `casks-system-peripherals.nix`

Edit the lists directly when you want to add or remove packages.
