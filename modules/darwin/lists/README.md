# Homebrew lists

These lists come from the original `.Brewfile` and are kept separate for readability.

- `taps.nix`
- `brews.nix`
- `casks.nix`

Edit the lists directly when you want to add or remove packages.
